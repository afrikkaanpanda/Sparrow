<%= form_with model: comment, url: post_comments_path(comment.post) do |form|%>
  <%= form.text_area :body, rows: 4, class: "form-control" %>
  <%= form.submit "Add Comment", class: "btn btn-primary mt-3" %>
<% end %>